# 核心依赖（仅保留必要项）
# 仅适配《Mitigating LLM Hallucination via Behaviorally Calibrated Reinforcement Learning》baseline + 4个校准目标

# PyTorch（深度学习框架）
torch>=2.0.0
torchvision>=0.15.0

# Transformers（模型加载和推理）
transformers>=4.35.0

# Datasets（数据集加载）
datasets>=2.14.0

# TRL（强化学习训练）
trl>=0.7.4

# Accelerate（分布式训练）
accelerate>=0.24.0

# PEFT（参数高效微调）
peft>=0.6.0

# 评估依赖
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# 工具依赖
wandb>=0.15.0  # 训练监控
tqdm>=4.65.0   # 进度条
sentencepiece>=0.1.99  # Qwen模型的分词器

# 日志和配置
pyyaml>=6.0
tensorboard>=2.14.0

# ============================================================
# 删除的TruthRL冗余依赖说明
# ============================================================

# ❌ vllm>=0.2.0
#    - TruthRL用于推理加速
#    - 目标论文使用标准transformers即可
#    - 删除原因：过度复杂，baseline不需要

# ❌ sglang>=0.1.0
#    - TruthRL特定的推理框架
#    - 目标论文不需要
#    - 删除原因：与目标论文无关

# ❌ faiss-cpu>=1.7.4
#    - TruthRL的检索增强（FAISS索引）
#    - 目标论文无RAG逻辑
#    - 删除原因：与目标论文无关

# ❌ opentelemetry-sdk>=1.26.0
#    - TruthRL的监控工具
#    - 目标论文不需要
#    - 删除原因：冗余依赖

# ❌ tensordict>=0.8.1
#    - TruthRL的张量管理
#    - 目标论文不需要
#    - 删除原因：冗余依赖

# ❌ e2b>=0.15.0
#    - TruthRL的代码执行（E2B沙箱）
#    - 目标论文仅关注数学问题，无代码评估
#    - 删除原因：与目标论文无关

# ❌ morph-client>=0.1.0
#    - TruthRL的代码执行（MorphCloud）
#    - 目标论文不需要
#    - 删除原因：与目标论文无关

# ❌ piston-client>=0.1.0
#    - TruthRL的代码执行（Piston）
#    - 目标论文不需要
#    - 删除原因：与目标论文无关

# ❌ math-verify>=0.1.0
#    - TruthRL的LaTeX验证库
#    - 目标论文使用简单字符串匹配
#    - 删除原因：过度复杂

# ❌ lighteval>=0.3.0
#    - TruthRL的评估框架
#    - 目标论文使用自定义评估逻辑
#    - 删除原因：与目标论文无关

# ❌ ray>=2.0.0
#    - TruthRL的分布式训练（Ray）
#    - 目标论文使用标准Accelerate即可
#    - 删除原因：过度复杂

# ❌ deepspeed>=0.12.0
#    - TruthRL的分布式训练优化
#    - 目标论文可选使用（不强制）
#    - 删除原因：可选依赖，不强制安装
